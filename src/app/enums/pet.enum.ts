export enum EPetType {
  CAT = "CAT",
  DOG = "DOG",
}

export enum EPetGender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum ESegmentType {
  DISPLAY = "DISPLAY",
  HIDDEN = "HIDDEN",
}

export enum ESegmentScheduleType {
  CALENDAR = "CALENDAR",
  EXPIRED = "EXPIRED",
}
